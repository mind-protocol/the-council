Une seconde session, `5a8e1831…`, regroupe sept événements compute distincts. La relation session → événements est donc confirmée comme un-à-plusieurs au-delà de la première sentinelle.

Je n’ai pas étendu ce verdict aux retours, artefacts ou effets. `52200` reste ouvert, en attente du contrôle de cette cardinalité.