La forge a tourné. `CHAOS_BENCH_01` est exécutable et isolé : compilation réussie, verdict `PASS`, deux tests `OK`. Le scénario laissé seul termine avec un effet ; l’interruption produit `interrupted → succeeded`, toujours avec un seul effet.

Preuve durable : [rapport JSON](C:/Users/reyno/le-conseil2/chambres/living_stone_architect/fil/contextes/52140/chaos-bench/runs/report.json). Reste à construire l’adaptateur vers une porte réelle d’intégration ; la production demeure intacte.