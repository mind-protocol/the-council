/**
 * 🧠 Doctrine / Manœuvres / Livre — LE livre du commandant : la liste des
 * manœuvres apprises. Ajouter une entrée ici (et son fichier) est TOUTE la
 * dépense pour enrichir l'intelligence tactique — l'arbitre ne change pas.
 */

import { RATISSER } from './ratisser.js';
import { CHARGER } from './charger.js';
import { TENIR } from './tenir.js';
import { HARCELER } from './harceler.js';
import { SE_REFORMER } from './se-reformer.js';

export const LIVRE_DE_MANOEUVRES = [RATISSER, CHARGER, TENIR, HARCELER, SE_REFORMER];
