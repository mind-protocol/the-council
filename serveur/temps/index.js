// LA PORTE serveur du container temps (docs/organisation.md §2) : sa route,
// requise par l'assembleur (serveur.js) — jamais en direct.
module.exports = {
  calendrier: require("../routes/calendrier"), // /calendrier
};
